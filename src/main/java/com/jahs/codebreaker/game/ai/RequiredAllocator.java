package com.jahs.codebreaker.game.ai;

public class RequiredAllocator {

    public RequiredAllocationParams allocateRequiredIfPossible(RequiredAllocationParams params) {
        // if there's a field where one of the options is a required color, and this color
        // is listed ONLY on this field, then simply select


        return params;
    }

}
